# Temporary files
*.tmp
*.log
*.bak
*~
.DS_Store

# Binary artifacts
*.xlsx

# Claude temporary files
tmpclaude-*

# Editor directories and files
.vscode/*
!.vscode/settings.json
!.vscode/mcp.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# OS files
Thumbs.db
.DS_Store

# Node modules (if any)
node_modules/
npm-debug.log*

# Python virtual environment
.venv/
venv/
env/

# Python cache (if any)
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.so

# Build artifacts (if any)
dist/
build/
*.egg-info/

# Local configuration
.env
.env.local
*.local

# AgentX runtime data (generated by installer/CLI)
.agentx/config.json
.agentx/version.json
.agentx/issues/
.agentx/digests/
.agentx/state/
.agentx/sessions/
.agentx/memory/
.agentx/handoffs/

# Cross-process file lock files (created by JsonFileLock / Lock-JsonFile)
*.lock

# AgentX output directories - tracked in Git (agent deliverables)
# NOTE: These directories are intentional deliverables created by agent workflows.
# They MUST NOT be gitignored or workflow jobs using 'git add' will fail.
# See: PM -> docs/artifacts/prd/, Architect -> docs/artifacts/adr/ + docs/artifacts/specs/,
#      UX -> docs/ux/, DevOps -> docs/deployment/,
#      Reviewer -> docs/artifacts/reviews/

# Cache directories
.cache/
.pytest_cache/
.mypy_cache/
