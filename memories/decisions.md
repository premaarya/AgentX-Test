- 2026-03-08: First harness runtime slice uses `.agentx/state/harness-state.json` beside the existing loop state file instead of replacing loop-state handling.
- 2026-03-08: Complex-work harness enforcement is centralized in `scripts/check-harness-compliance.ps1` and invoked by `.github/workflows/quality-gates.yml`.
- 2026-03-08: Provider selection is now normalized through `provider` with fallback to legacy `integration` and `mode` fields to preserve old workspaces.
- 2026-03-08: Provider-aware issue mutations now execute against GitHub (`gh`) and ADO (`az boards work-item`) instead of degrading to local issue files; live ADO validation remains environment-dependent.
- 2026-03-08: GitHub Project V2 status sync is handled in CLI scope only for the `Status` single-select field and only when a GitHub project number is explicitly configured.
- 2026-03-08: `.agentx/agentx.ps1` now explicitly propagates child exit codes from `.agentx/agentx-cli.ps1` so wrapper-based automation can detect GitHub and local failure paths correctly.
- 2026-03-08: Agent X is no longer modeled as delegation-only. The repo contract now treats Agent X as the autonomous top-level executor that applies specialist phases internally and only falls back to manual switching when isolation or platform behavior requires it.
- 2026-03-08: `scripts/validate-frontmatter.ps1` now treats YAML block scalars like `description: >-` as first-class frontmatter values instead of truncating them, so skill validation reflects actual metadata rather than parser artifacts.
- 2026-03-09: `vscode-extension/src/agentxContext.ts` remains a live runtime surface and must parse multiline frontmatter lists correctly; the `getList()` regex now uses escaped whitespace tokens and is covered by a regression test.
- 2026-03-09: Historical architecture docs remain in-repo for traceability, but current runtime truth lives in `AGENTS.md`, `docs/WORKFLOW.md`, `docs/GUIDE.md`, and the live `vscode-extension/src/` tree; superseded migration/spec/ADR/PRD docs should be clearly marked as archival only.
- 2026-03-09: `AgentX Auto` is the autonomous execution mode name for the top-level orchestrator only; product branding, CLI ids, package ids, repo paths, and `.agentx/` technical identifiers remain `AgentX`.
- 2026-03-09: `.agentx/agentic-runner.ps1` now honors frontmatter `modelFallback` as a real fallback chain for model-availability failures: preferred model -> fallback list -> default model.
- 2026-03-09: CLI context compaction in `.agentx/agentic-runner.ps1` is now token-threshold aware using approximate token estimation and a default 70% context-window trigger keyed off the active model id, while still preserving recent messages and system prompts.
- 2026-03-09: CLI context compaction is now applied before every LLM call and is driven only by the 70% token budget threshold, not by message-count limits.
- 2026-03-09: Clarification loops now return a structured clarification handoff summary, not just the raw answer, and responder prompts include recent discussion history to support richer inter-agent follow-up.
- 2026-03-09: VS Code chat now streams live CLI runner events instead of buffering them until completion, so compaction, clarification, tool activity, and self-review are visible in chat with the same runtime path as the CLI.
- 2026-03-09: Human clarification in VS Code chat now uses resumable AgentX sessions instead of terminal stdin, preserving the CLI clarification workflow while adapting it to Copilot Chat's non-interactive process model.
- 2026-03-09: VS Code chat now writes full AgentX run output to an `AgentX Chat` output channel and only inlines a compact preview in chat when stdout is large, reducing outer Copilot chat context growth without losing access to raw output.
- 2026-03-09: `install.ps1` now installs `AGENTS.md` and `Skills.md` and creates `docs/ux`, `docs/reviews`, and `docs/progress` so the live installer output matches the install test suite's required runtime artifacts.
- 2026-03-09: `vscode-extension/src/commands/initialize.ts` now installs the same core reference docs and runtime scaffold the main installer creates, including `AGENTS.md`, `Skills.md`, `docs/GUIDE.md`, `docs/WORKFLOW.md`, `docs/architecture`, and memory directories.
- 2026-03-09: The AgentX sidebar now uses six top-level operational views (`Work`, `Agents`, `Workflow`, `Templates`, `Quality`, `Integrations`) instead of surfacing only static library-style sections; contextual skills belong under `Agents`, not as a seventh top-level view.
- 2026-03-09: UX Designer now declares `Gemini 3.1 Pro (Preview) (copilot)`, and `.agentx/agentic-runner.ps1` explicitly maps preview-labeled Gemini strings so agent frontmatter labels and runner model resolution stay aligned.
- 2026-03-09: Release stamping for 8.2.2 is kept aligned across root installer/version surfaces, pack manifests, extension metadata, and version-sensitive tests; the VSIX is produced from `npm run package` in `vscode-extension/` so bundled `.github/agentx` assets are refreshed from repo state before packaging.
- 2026-03-10: Azure companion integration now uses the Azure MCP Extension (`ms-azuretools.vscode-azure-mcp-server`) as the primary install path because it brings in the Azure Skills plugin plus Azure MCP and Foundry MCP together; AgentX no longer models Azure support as an always-on hard extension dependency.
- 2026-03-10: When AgentX detects that a local workspace has moved to GitHub, it now auto-persists the GitHub provider and runs a one-time full backlog migration that carries local title/body/labels/comments plus the latest workflow state into remote GitHub issues instead of relying on a manual `gh issue create` loop.
- 2026-03-10: AgentX now exposes a dedicated `backlog-sync github --force` CLI command so users can re-sync already-mapped local backlog items to GitHub on demand, including reopened issues and new local comments, without waiting for provider inference.
- 2026-03-10: VS Code extension packaging must bundle the root `.agentx` runtime files and `agentx.initialize` must install those same scripts, or CLI features like GitHub backlog migration and `backlog-sync` will drift between root installs and extension-based installs.
- 2026-03-10: AgentX Auto internal specialist phases now explicitly preserve the corresponding role contract: when acting as PM, Architect, UX, Engineer, Reviewer, or another specialist, it must load that agent definition and cannot skip the role's required constraints, templates, skills, entry gates, exit gates, or deliverables.
- 2026-03-12: Harness evaluation should be added as an AgentX-native, advisory-first layer over existing session, loop, harness, and validation artifacts; phase 1 stays additive, deterministic-check driven, and must avoid external repository references in repo artifacts.
- 2026-03-12: Harness evaluation architecture is now formalized in `docs/artifacts/adr/ADR-Harness-Evaluation-Adoption.md` and `docs/artifacts/specs/SPEC-Harness-Evaluation-Adoption.md`; the chosen direction is a deterministic, native evaluation layer with attribution and observation-coverage reporting, kept advisory-first before any CI gate promotion.
- 2026-03-12: The first shipped harness evaluator lives in the VS Code extension as a deterministic artifact reader over execution plans, progress logs, loop state, and harness state; it reports score, coverage, and dominant attribution in the existing Quality sidebar before any standalone command or CI gate is introduced.
- 2026-03-12: The adoption analysis was distilled into a single initiative captured in `docs/artifacts/prd/PRD-157.md` and GitHub epic `#157`; the chosen scope is explicit knowledge compounding, metadata-driven learnings retrieval, agent-native review, workflow contracts, and durable review-finding tracking, with implementation starting from workflow/schema contracts before UI or automation layers.
- 2026-03-12: Story `#163` is now anchored by `docs/artifacts/adr/ADR-163.md` and `docs/artifacts/specs/SPEC-163.md`; the chosen contract is a five-phase lifecycle (plan, design, execute, review, compound capture) over existing AgentX artifact families, with post-review capture classified as mandatory, optional, or skip.
- 2026-03-12: Story `#162` is now anchored by `docs/artifacts/adr/ADR-162.md` and `docs/artifacts/specs/SPEC-162.md`; durable `LearningRecord` artifacts are intentionally separate from runtime observations, and phase-1 retrieval must remain metadata-first and deterministically ranked by category, lifecycle relevance, keyword overlap, evidence strength, validation state, recency, and reuse signal.
- 2026-03-12: Story `#166` now uses the shared workflow guide at `docs/guides/KNOWLEDGE-REVIEW-WORKFLOWS.md` plus curated `docs/artifacts/learnings/LEARNING-*.md` artifacts to define advisory-first post-review capture guidance, mandatory/optional/skip trigger rules, and artifact location/link-back expectations.
- 2026-03-12: Story `#169` now exposes deterministic ranked learnings retrieval in the VS Code extension through `agentx.showPlanningLearnings`, `agentx.showReviewLearnings`, Work view actions, and chat prompts (`learnings planning`, `learnings review`, `capture guidance`) backed by `vscode-extension/src/utils/learnings.ts`.
- 2026-03-12: Story `#165` now defines agent-native review as a deterministic parity audit over action parity, context parity, and shared workspace expectations, with the capability map and scoring rubric anchored in `docs/guides/KNOWLEDGE-REVIEW-WORKFLOWS.md` and reusable guidance captured in `docs/artifacts/learnings/LEARNING-165.md`.
- 2026-03-12: Story `#168` now surfaces advisory agent-native review in the VS Code extension through `agentx.showAgentNativeReview`, Quality sidebar summary rows, and the `@agentx agent-native review` chat entry point, all backed by `vscode-extension/src/review/agent-native-review.ts`.
- 2026-03-12: Stories `#164` and `#167` now use a repo-local durable finding model under `docs/artifacts/reviews/findings/` with standard AgentX workflow statuses, a shared guide at `docs/guides/KNOWLEDGE-REVIEW-WORKFLOWS.md`, and promotion into normal AgentX issues instead of any parallel tracker.
- 2026-03-12: To reduce documentation clutter after the initial rollout, the separate capture-guidance, agent-native-review, and review-findings guides were consolidated into `docs/guides/KNOWLEDGE-REVIEW-WORKFLOWS.md`, and the completed harness evaluation planning artifacts under `docs/plans/` and `docs/progress/` were removed after their durable design content was preserved in the ADR, spec, and shipped extension code.
- 2026-03-13: The extension cleanup should keep original public modules as stable facades and move deterministic or private workflows into adjacent `*Internals.ts`, `*Engine.ts`, or `*Types.ts` files. For this cleanup pass, that public-surface stabilization is now considered complete after the `initialize.ts` and `shell.ts` extraction slice; remaining larger files are either intentional runtime facades or internal implementation modules.
- 2026-03-13: `setupWizard.ts` and `requestRouter.ts` should follow the same facade-first pattern as the earlier utility and review slices, and `agentxContext.ts` should keep only the stateful class surface while pure resolution and enumeration logic lives in `agentxContextInternals.ts`. After this final batch, further decomposition is optional and should be driven by cohesion or testing value rather than line-count cleanup alone.
- 2026-03-12: The aggressive cleanup pass removed the superseded archive `docs/architecture/ARCH-AgentX.md` and the unreferenced external adoption review under `docs/artifacts/reviews/`, then rewired archival PRD/ADR/spec references to `AGENTS.md` and `docs/architecture/MIGRATION-PLAN.md` as the remaining canonical current-state and migration context.
- 2026-03-12: Review-finding promotion now creates normal AgentX issues through `.agentx/agentx-cli.ps1 issue create` using new base64-backed `--title-base64` and `--body-base64` inputs so VS Code commands and chat can safely promote multiline finding content without shell-quoting failures.
- 2026-03-12: The vendored extension docs under `vscode-extension/.github/agentx/` must use bundle-local links such as `skills/...`, `agents/...`, and `templates/...`; repo-root `.github/...` paths are valid in source docs but break the packaged markdown reference validator.
- 2026-03-12: UI/UX adoption from external inspiration should land as original AgentX-native guidance, not copied content; the chosen addition is a new `design-system-reasoning` skill that bridges product posture, page archetypes, anti-pattern filters, token guidance, and stack translation between existing UX methodology and frontend implementation skills.
- 2026-03-12: The second-pass enrichment for `design-system-reasoning` adds reusable industry presets plus a structured design review scorecard, keeping detailed examples in `references/` and `assets/` so the main skill stays a routing and reasoning layer rather than turning into a monolithic style catalog.
- 2026-03-12: Strengthening AgentX's compound engineering loop should focus on first-class operator surfaces, not copying external worktree or provider-conversion mechanics. The adopted shape is explicit `brainstorm` and `compound` entry points plus a one-click learning-capture scaffold tied to the active harness thread and issue context.
- 2026-03-12: The first repo-structure cleanup slice standardizes living execution artifacts under `docs/execution/plans/` and `docs/execution/progress/`, keeps `docs/artifacts/prd/`, `docs/artifacts/adr/`, `docs/artifacts/specs/`, and `docs/artifacts/reviews/` stable for now, and leaves `docs/plans/` plus `docs/progress/` as redirect-only shims to reduce migration risk across agents, prompts, schemas, and historical docs.
- 2026-03-12: The second repo-structure cleanup slice consolidates durable workflow artifacts under `docs/artifacts/` (`prd/`, `adr/`, `specs/`, `reviews/`, `learnings/`), updates agent/prompt/workflow/runtime contracts to those canonical paths, and leaves the old top-level artifact directories as redirect-only shims.
- 2026-03-13: The third extension structure slice keeps `vscode-extension/src/utils/commandValidator.ts` as the stable facade while moving its types, policy tables, and shared helpers into adjacent `commandValidator*` modules.
- 2026-03-13: The fourth extension structure slice keeps `vscode-extension/src/utils/harnessState.ts` as the stable facade while moving its type definitions and internal storage/helper logic into adjacent `harnessState*` modules.
- 2026-03-13: The fifth extension structure slice keeps `vscode-extension/src/utils/learnings.ts` as the stable facade while moving its exported types and internal parsing/scoring helpers into adjacent `learnings*` modules.
- 2026-03-13: The sixth extension structure slice keeps `vscode-extension/src/utils/dependencyChecker.ts` as the stable facade while moving its exported report/types and internal probe/helper logic into adjacent `dependencyChecker*` modules.
- 2026-03-13: The seventh extension structure slice keeps `vscode-extension/src/utils/ssrfValidator.ts` as the stable facade while moving its validation result type and internal policy/helper logic into adjacent `ssrfValidator*` modules.
- 2026-03-13: The eighth extension structure slice keeps `vscode-extension/src/review/review-findings.ts` as the stable facade while moving its exported finding contracts and internal parsing/persistence helpers into adjacent `review-findings*` modules.
- 2026-03-13: The next workflow-expansion initiative should stay repo-native and additive: unify brainstorm -> plan -> work -> review -> compound surfaces first, then layer task bundles, bounded parallel delivery, skill packaging, and generated host portability from canonical AgentX assets.
- 2026-03-13: Epic `#215` is now architected as a layered expansion: a deterministic workflow control plane first, then task bundles and bounded parallel delivery, then skill-packaging uplift and canonical portability generation. AI remains advisory over artifact-based workflow state rather than defining the control plane.
- 2026-03-13: Phase-one workflow feature issues should list explicit GitHub story numbers in their bodies and repo docs should mirror the live hierarchy exactly; title-only story sets made #214 and PRD-215 drift after creation order changed.
- 2026-03-13: The clarification backlog under issues `#130`-`#156` belongs to the superseded v7.x ledger-and-locking architecture. Current clarification truth is the declarative migration plus the synchronous runner/chat flow, so those legacy issues were retired instead of being kept as active backlog.
- 2026-03-13: Epic `#157` and features `#158`-`#161` were closed as completed after the knowledge-compounding, learnings, agent-native review, and durable review-finding stories shipped; leaving delivered planning issues open was backlog noise.
- 2026-03-13: Epic `#215` issue body needed a live-hierarchy repair even after the phase-one feature/story bodies were corrected; the epic now lists the actual story numbers for workflow checkpoints, entry points, recommendations, rollout, pilot order, and operator enablement.
- 2026-03-13: Phase-one implementation for epic `#215` uses one shared workflow-guidance engine in `vscode-extension/src/utils/workflowGuidance.ts`; Work sidebar, Status sidebar, commands, and chat all consume the same normalized checkpoint/recommendation contract instead of each surface inferring workflow state independently.
- 2026-03-13: Story `#220` is completed by making `docs/WORKFLOW.md` the canonical checkpoint contract for `Brainstorm`, `Plan`, `Work`, `Review`, `Compound Capture`, and `Done`; the compound-review guide extends that contract but does not define a parallel lifecycle vocabulary.
- 2026-03-13: Story `#230` is anchored by `docs/artifacts/specs/SPEC-230.md`; task bundles remain repo-local subordinate artifacts with minimum required metadata for parent context, priority, state, owner, evidence links, promotion mode, and timestamps, and they reuse standard workflow states plus only `Proposed` and `Archived` as bundle-only substates.
- 2026-03-13: Story `#225` is anchored by `docs/artifacts/specs/SPEC-225.md`; bundle-item promotion remains local by default, promotes only into normal issue hierarchy or durable review findings when justified, links to existing durable records by default to avoid duplicates, and preserves searchable local history after promotion.
- 2026-03-13: Story `#229` is anchored by `docs/artifacts/specs/SPEC-229.md`; bounded parallel delivery is fail-closed and only eligible when scope independence, dependency coupling, artifact overlap, review complexity, and recovery complexity are all acceptable before any task-unit creation begins.
- 2026-03-13: Story `#224` is anchored by `docs/artifacts/specs/SPEC-224.md`; isolated task units are subordinate artifacts with explicit scope, owner, isolation mode, status, merge readiness, recovery guidance, and parent-summary signals, with merge readiness modeled separately from unit status.
- 2026-03-13: After completing the phase-two p1 architect slice, feature-level backlog comments were added to #227 and #226 and an epic-level summary comment was added to #215 because Project V2 status mutation is not available through the current GitHub tool surface; comment-based traceability is the explicit fallback.
- 2026-03-13: Story `#223` is anchored by `docs/artifacts/specs/SPEC-223.md`; task-bundle create, list, resolve, and promote behavior is defined as a shared action contract across extension and CLI surfaces, with active issue/plan context resolution and fail-closed hierarchy safeguards.
- 2026-03-13: Story `#228` is anchored by `docs/artifacts/specs/SPEC-228.md`; bounded parallel work now has a mandatory reconciliation checkpoint covering overlap, conflicts, acceptance evidence, approval, and follow-up routing before parent closure can proceed into the normal review and compound-capture lifecycle.
- 2026-03-13: After stories `#223` and `#228`, both phase-two features are architecturally complete: feature `#227` now has metadata, promotion, and operator-surface contracts; feature `#226` now has eligibility, task-unit, and reconciliation contracts. Epic `#215` now has full phase-one and phase-two architect coverage in repo artifacts.
- 2026-03-13: Feature `#227` implementation is CLI-first: `.agentx/agentx-cli.ps1` owns the repo-local `bundle` contract (`create`, `list`, `get`, `resolve`, `promote`) and the VS Code extension uses thin command wrappers over that shared CLI behavior to avoid surface drift.
- 2026-03-13: Repo-local task bundles are stored as JSON records under `docs/execution/task-bundles/`, keeping them visible and file-backed while leaving normal issues and durable review findings as the source of truth for promoted work.
- 2026-03-13: Feature `#226` implementation is also CLI-first: `.agentx/agentx-cli.ps1` owns the durable `parallel` contract (`assess`, `start`, `list`, `get`, `reconcile`), and the VS Code extension exposes thin bounded-parallel facades and commands from `src/parallel/` to keep extension and CLI behavior aligned.
- 2026-03-14: Workflow parity remediation issue `#232` now uses `vscode-extension/src/utils/workflowGuidanceEngine.ts` as the shared deterministic checkpoint/recommendation engine while `workflowGuidance.ts` remains the public facade consumed by chat, commands, and sidebars.
- 2026-03-14: Workflow parity remediation issue `#232` also uses `docs/guides/AGENT-COMMUNICATION-PROTOCOL.md` as the canonical clarification/escalation guide, while CLI and docs were aligned to the live positional `deps` and `workflow` forms instead of adding compatibility aliases.
- 2026-03-14: GitHub routing parity should stay incremental: `.github/workflows/agent-x.yml` now routes status-first only on manual dispatch when `.agentx/config.json` provides a project number, while issue-open automation remains label-based because Project V2 status changes do not emit normal issue events.
- 2026-03-14: The supported in-repo reroute path for GitHub Project V2 status moves is an explicit issue comment `/agentx route`; it keeps routing inside normal GitHub events without adding compatibility aliases or external orchestration.
- 2026-03-14: Full GitHub-side rerouting for Project V2 board moves uses a separate scheduled poller workflow (`.github/workflows/agent-x-project-reroute.yml`) that scans recent project-item updates and redispatches `agent-x.yml`; `/agentx route` remains the immediate fallback between polls.
- 2026-03-17: Release stamping for 8.3.8 should update the root installer/version surfaces, pack manifests, extension metadata, bundled `.github/agentx` assets, and version-sensitive tests together, while leaving historical memory notes unchanged; extension `out/` artifacts should be refreshed by recompilation rather than manual edits.
- 2026-03-15: Post-E2E hardening keeps the existing CLI surfaces but fixes real sandbox failures at the contract boundaries: `.agentx/agentx.ps1` now normalizes success exit codes to `0`, `workflow` accepts issue-type aliases in addition to agent names, installers explicitly bundle the core reference docs, and `scripts/score-output.ps1` is resilient to scalar collection results and canonical `docs/artifacts/*` paths.
- 2026-03-15: Fresh installers must not copy mutable `.agentx` runtime state from the source archive. `install.ps1` and `install.sh` now exclude source `config.json`, `version.json`, issue/digest/session/memory data, and `.agentx/state/*` so new workspaces always generate clean runtime state from the selected install mode.
- 2026-03-16: The 8.3.1 release stamp is aligned across root installers/docs/manifests, pack manifests, extension metadata, bundled extension assets, and version-sensitive tests; extension validation passed with `npm test` in `vscode-extension/` at `447 passing` after updating stale evaluator/sidebar fixtures to the canonical `docs/execution/plans` and `docs/execution/progress` layout.
- 2026-03-16: The 8.3.2 release stamp follows the same source-of-truth surfaces as 8.3.1: root installers/docs/manifests, pack manifests, extension metadata, bundled extension assets, and version-sensitive tests are kept in sync before the release commit.
- 2026-03-16: Stale-open issue prevention now uses three layers: repo docs require closing keywords on final GitHub delivery, `.github/hooks/commit-msg` warns when a commit only links an issue, and `.github/workflows/issue-closeout-audit.yml` comments on open issues referenced by delivered commits on `master` when no closing keyword was used.
- 2026-03-16: VS Code extension startup dependency checks are advisory-only. Activation now warns about missing required tools and routes users to `AgentX: Check Environment` instead of auto-starting terminal installs or long polling flows during activation.
- 2026-03-16: Windows PowerShell detection in the extension now falls back to standard `C:\Program Files\PowerShell\7*\pwsh.exe` locations so fresh installs can be recognized even before PATH refreshes inside the current VS Code process.
- 2026-03-16: Technical specs now require an explicit selected tech stack before implementation. `.github/templates/SPEC-TEMPLATE.md` includes a required stack-selection table, and the Architect agent contract now treats unresolved stack choices as a blocker to implementation handoff.
- 2026-03-17: The VS Code extension command surface is now local-first: `agentx.initializeLocalRuntime` installs the repo-local runtime, while `agentx.addRemoteAdapter` configures GitHub or ADO separately instead of mixing runtime bootstrap with provider setup.
- 2026-03-17: The CLI now treats GitHub and ADO as additive adapters when the workspace is in local mode. Repository metadata and backlog sync state can be recorded without auto-promoting a remote provider over the local runtime.
- 2026-03-17: Remote detection policy changed again: workspace initialization always installs the full local runtime first, but once a GitHub or Azure DevOps `origin` is detected the extension should silently add the matching adapter and switch `provider`/`integration`/`mode` to that remote while keeping the local runtime available underneath.
- 2026-03-17: Release 8.4.0 should be stamped from source-of-truth files first, then propagated to bundled `.github/agentx` assets, compiled extension outputs, and the VSIX through rebuild steps rather than manual edits to generated files.