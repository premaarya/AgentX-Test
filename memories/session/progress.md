 2026-03-18: Completed the zero-copy installed-runtime slice. `agentx.initializeLocalRuntime` now bootstraps only mutable workspace state/artifact folders, the extension runs the bundled `.github/agentx/.agentx` runtime with `AGENTX_WORKSPACE_ROOT`, the PowerShell runtime resolves default agents from installed assets, and focused validation passed with clean TypeScript compile plus a bundled-runtime smoke test. Four unrelated extension tests still fail outside this slice.
 2026-03-19: Added wrapper-based workspace entrypoints on top of the zero-copy runtime model. `vscode-extension/src/commands/initializeInternals.ts` now generates `.agentx/agentx.ps1`, `.agentx/agentx.sh`, and local issue-manager wrappers that delegate to the installed extension runtime and preserve `AGENTX_WORKSPACE_ROOT`; `runInitializeLocalRuntimeCommand` writes them during bootstrap; targeted extension compile and mocha validation passed.
- 2026-03-24: Fixed the failing installer and extension validation surfaces. `install.ps1` and `install.sh` now support deterministic archive overrides, `tests/test-install.ps1` builds a local archive fixture and passed at `138/138`, branch-focused tests were added for task-bundle, parallel-delivery, and work-tree helper paths, and the extension coverage gate now passes with actual totals of `81.19` statements/lines, `84.62` functions, and `73.9` branches.
- 2026-03-24: Started the Backlog local-provider migration. Added `docs/execution/plans/EXEC-PLAN-backlog-local-provider.md`, refactored `.agentx/agentx-cli.ps1` to use provider CRUD/list/update helper seams, and validated with `tests/test-framework.ps1`, `.agentx/agentx.ps1 issue list`, and `.agentx/agentx.ps1 ready`.
- 2026-03-24: Next step is implementing the Backlog-backed local provider behind the new seam while preserving existing GitHub and ADO adapter behavior.
- 2026-03-24: Implemented the Backlog-backed local provider inside `.agentx/agentx-cli.ps1` with markdown task parsing/serialization, numbering/dependency/priority mapping, and comment round-tripping. Fixed two validation-found edge cases: empty-result probing during backend detection and wildcard characters in title-derived file paths.
- 2026-03-24: Validation is green after the Backlog slice: `pwsh -NoProfile -File .\tests\provider-behavior.ps1` passed `49/49`, and `pwsh -NoProfile -File .\tests\test-framework.ps1` passed `125/125`. Next step is doc/config cleanup for the new local backend surface.
- 2026-03-25: Revalidated the backlog-provider follow-up after the `priority:p0` and locked numbering fixes. `tests/provider-behavior.ps1` passed `51/51`, related behavior suites passed (`task-bundle` 16/16, `bounded-parallel` 13/13, `harness-audit` 10/10, `agentic-runner` 62/62), and isolated reruns of `tests/test-framework.ps1` returned `EXIT=0`, so the earlier aggregate-wrapper failure note is stale.
- 2026-03-13: Created `docs/execution/plans/COMPOUND-WORKFLOW-AND-PORTABILITY-PLAN.md` and `docs/execution/progress/COMPOUND-WORKFLOW-AND-PORTABILITY-PROGRESS.md` with a six-workstream backlog covering workflow cohesion, bounded parallel delivery, task bundles, skill packaging, portability, and rollout governance.
- 2026-03-13: Created GitHub epic `#215`, features `#214` and `#216`, stories `#217` through `#222`, and durable PRD `docs/artifacts/prd/PRD-215.md` for the phase-1 workflow-cohesion and rollout slice.
- 2026-03-13: Added `docs/artifacts/adr/ADR-215.md` and `docs/artifacts/specs/SPEC-215.md`, grounding the initiative architecture in PRD-215, the compound workflow guide, and the execution backlog.
- 2026-03-13: Drafted the implementation plan for story `#219` and created the second-wave GitHub backlog: features `#227` and `#226` plus stories `#230`, `#225`, `#223`, `#229`, `#224`, and `#228`.
- 2026-03-13: Corrected the phase-one story-number mapping drift between PRD-215 and the live GitHub backlog, and added execution plans plus progress logs for stories `#220`, `#221`, and `#218` so the remaining workflow-control stories are implementation-ready.
- 2026-03-13: Added story-level architect specs for all six phase-one stories under epic `#215`: `SPEC-217.md`, `SPEC-218.md`, `SPEC-219.md`, `SPEC-220.md`, `SPEC-221.md`, and `SPEC-222.md`, using `ADR-215.md` plus `PRD-215.md` as the shared architecture and product source of truth.
- 2026-03-13: Queried the live p0 backlog, selected the oldest executable story `#137`, and implemented its machine-readable clarification ledger contract by adding `.github/schemas/clarification-ledger.schema.json` plus canonical references from `PRD-AgentX.md` and `SPEC-AgentX.md`.
- 2026-03-13: Audited the live GitHub backlog against current architecture. Fixed the stale phase-one story mapping in epic `#215`, closed delivered knowledge-compounding planning issues `#157`-`#161` as completed, closed story `#137` as completed, and retired the superseded clarification initiative issues `#130`-`#136` and `#138`-`#156` as not planned because they target the archived v7.x clarification ledger/router design.
- 2026-03-13: Implemented the phase-one workflow-cohesion slice for epic `#215` in the VS Code extension. Added shared checkpoint/recommendation logic in `vscode-extension/src/utils/workflowGuidance.ts`, surfaced it through Work and Status sidebars, commands, and chat, added durable rollout-support guides under `docs/guides/`, expanded focused tests, and finished with `npm test` in `vscode-extension/` passing at `436 passing (28s)`.
- 2026-03-13: Completed story `#220` by publishing the canonical checkpoint contract in `docs/WORKFLOW.md`, cross-linking `docs/guides/KNOWLEDGE-REVIEW-WORKFLOWS.md`, and updating the story plan/progress artifacts to complete. The remaining closeout step is GitHub issue closure for `#220` and parent feature `#214` after validation.
- 2026-03-13: Started phase-two architecture under epic `#215` with story `#230`. Added `docs/artifacts/specs/SPEC-230.md` plus matching execution plan/progress artifacts to define the minimum repo-local task-bundle metadata contract before promotion-path and operator-surface stories.
- 2026-03-13: Completed the first p1 phase-two architect slice under epic `#215` in the requested order. Added `SPEC-225.md`, `SPEC-229.md`, and `SPEC-224.md` plus matching execution plan/progress artifacts, finishing the promotion-path, bounded-parallel eligibility, and isolated-task-unit contracts on top of the completed `SPEC-230.md` metadata baseline.
- 2026-03-13: Added feature-level architect completion comments to GitHub features #227 and #226 and an epic-level summary comment to #215, explicitly marking #223 and #228 as the next unblocked follow-on stories after the completed p1 architect slice.
- 2026-03-13: Continued with both remaining phase-two follow-on stories. Added `SPEC-223.md` and `SPEC-228.md` plus matching execution plan/progress artifacts, completing the feature-level design surfaces for both task-bundle operations and bounded parallel delivery.
- 2026-03-13: Validated `SPEC-223.md` and `SPEC-228.md`, marked their execution artifacts complete, and added story-, feature-, and epic-level GitHub comments confirming that the full phase-two architect surface for epic `#215` is now complete and ready for engineering handoff.
- 2026-03-08: Implemented harness Phase 2 and initial Phase 3 slice: CI plan enforcement, weekly plan-health reporting, file-backed extension harness state, loop command aliases, and unit tests.
- 2026-03-08: Validation completed with `npm test` in `vscode-extension/` and advisory execution of `scripts/check-harness-compliance.ps1`.
- 2026-03-08: Started platform parity slice to normalize provider resolution across Local, GitHub, and ADO and added an Azure Pipelines entrypoint for shared harness validation.
- 2026-03-08: Completed the current platform parity slice: provider-aware GitHub and ADO issue mutations are implemented, provider docs were normalized, and non-destructive CLI validation passed. Remaining gap is live ADO validation in an environment with Azure CLI installed.
- 2026-03-08: Added GitHub Project V2 status sync for configured GitHub providers. Validation covered live read-only `gh project` metadata queries; a live mutating status transition is still pending because this workspace is not configured against a project number.
- 2026-03-08: Completed live GitHub Project V2 status-sync validation using temporary issue #128 in project 4, then restored `.agentx/config.json` to its original local-mode state. The remaining operational gap is live ADO validation only.
- 2026-03-08: Finished the Local/GitHub deep-review fixes. GitHub CLI failures now fail loudly, GitHub ready respects project `Ready` when project sync is configured, the PowerShell launcher propagates child exit codes, and `tests/provider-behavior.ps1` passes 18/18.
- 2026-03-08: Updated Agent X from a manual delegation-only contract to an autonomous orchestration contract for both simple and complex tasks. Core workflow docs, Claude command wiring, onboarding guide, and the extension integration test helper were aligned with the new behavior model.
- 2026-03-08: Cleaned up the remaining validation drift: fixed the frontmatter parser for block scalar descriptions, updated stale framework test expectations, corrected README skill counts/wording, and reached `tests/test-framework.ps1` = 100/100 passing.
- 2026-03-09: Continued the repo-wide cleanup pass by fixing the live extension frontmatter list parser, adding a regression test for multiline list fields, marking historical migration/architecture/spec/ADR/PRD docs as archival-only, and aligning `docs/QUALITY_SCORE.md` plus `docs/tech-debt-tracker.md` with the actual extension tree.
- 2026-03-09: Validation after the cleanup passed with `npm --prefix .\vscode-extension test` and `pwsh -File .\tests\test-framework.ps1` (100/100).
- 2026-03-09: Updated the scoped autonomous-mode naming change so only the top-level execution agent is now `AgentX Auto` in frontmatter and execution-facing command text; product branding and technical identifiers remain `AgentX`. Validation pending rerun of `npm --prefix .\vscode-extension test`.
- 2026-03-09: Implemented real frontmatter `modelFallback` handling in `.agentx/agentic-runner.ps1` with availability-based failover to the next candidate model. PowerShell parser validation passed; no dedicated automated test currently covers this runner path.
- 2026-03-09: Implemented the remaining runner enhancements: token-aware compaction at 70% of the active model context window, richer clarification handoff summaries with discussion history, and dedicated regression tests in `tests/agentic-runner-behavior.ps1`. Validation passed with `pwsh -NoProfile -File .\tests\agentic-runner-behavior.ps1` and `pwsh -NoProfile -File .\tests\test-framework.ps1` (102/102).
- 2026-03-09: Refined runner compaction again so it now triggers before each LLM call and uses only the 70% token threshold, removing message-count gating. Validation pending rerun of focused runner tests and the framework suite.
- 2026-03-09: Started CLI/chat parity update for the extension by streaming live CLI runner events into chat progress, adding richer clarification detail lines in the runner, and adding extension tests for streaming shell output plus chat progress visibility. Validation pending extension and framework test reruns.
- 2026-03-09: Added resumable human clarification for Copilot Chat: the runner now records pending clarification state in saved sessions, the CLI can resume a session with `--resume-session` and `--clarification-response`, and chat exposes that flow through `@agentx continue "..."`. Validation pending extension and framework test reruns.
- 2026-03-09: Reduced outer Copilot chat bloat by compacting large final AgentX stdout into a short preview and mirroring the full transcript to the `AgentX Chat` output channel. Validation passed with `npm test -- --grep "chatParticipant"` in `vscode-extension/`.
- 2026-03-09: Completed a full tester pass across the main automated suites: extension `npm test` (393 passing), runner behavior (22/22), framework tests (102/102), provider behavior (23/23), memory scale (11/11). The installer suite exposed missing scaffold artifacts; `install.ps1` was fixed and the latest install-test scenario workspaces now contain the previously missing files and directories.
- 2026-03-09: Aligned the VS Code initializer with the main installer so extension-driven setup now installs the missing core docs and runtime folders too. Validation passed with `npm test` in `vscode-extension/` (393 passing).
- 2026-03-09: Implemented the six-view sidebar follow-on slice: `Agents` now shows curated suggested skills with direct `SKILL.md` links, `Work` now exposes direct active-thread issue/dependency/loop actions plus pending-clarification details, and `agentx.checkDeps` accepts an optional issue number for sidebar invocation. Validation passed with `npm test` in `vscode-extension/` (400 passing).
- 2026-03-09: Updated UX Designer model labeling so the agent now declares `Claude Sonnet 4.6 (copilot)`. The runner already contains explicit Claude Sonnet mappings, so frontmatter labels and runner model resolution remain aligned.
- 2026-03-09: Removed `GitHub.copilot` and `GitHub.copilot-chat` from `.vscode/extensions.json` so this repo no longer triggers the standard VS Code workspace recommendation prompt for those extensions. Validation passed with `get_errors` on `.vscode/extensions.json`.
- 2026-03-09: Verified install propagation: both `install.ps1` and `install.sh` copy the repo `.vscode/` folder into target workspaces, so removing Copilot recommendations from `.vscode/extensions.json` changes future AgentX installs as well. The installers still only auto-install the Azure companion extension.
- 2026-03-09: Fixed the remaining workspace-reported AgentX errors by removing unsupported frontmatter fields from `powerbi` and `prototype-craft` skills and correcting broken `references/` links in the `cognitive-architecture` skill. Focused `get_errors` checks passed for all three files, and the full workspace error scan now reports no errors.
- 2026-03-09: Completed the 8.2.2 release stamp across docs, installers, pack manifests, extension metadata, and version-sensitive tests. Packaging passed with `npm run package` in `vscode-extension/`, producing `vscode-extension/agentx-8.2.2.vsix`. Targeted diagnostics on the edited files reported no errors; commit/push was the remaining step at this checkpoint.
- 2026-03-10: Added Azure-aware companion setup across the installers, VS Code extension, and docs. AgentX now detects Azure-oriented workspaces, recommends or installs the Azure MCP Extension for Azure Skills support, removes the unconditional old Azure companion dependency, and validation passed with `npm test` in `vscode-extension/` (400 passing).
- 2026-03-10: Completed the 8.2.6 release stamp across installers, docs, packs, extension metadata, and version-sensitive tests. Validation passed with `npm test` and `npm run package` in `vscode-extension/`, producing `vscode-extension/agentx-8.2.6.vsix`, and `tests/test-install.ps1` emitted passing 8.2.6 installer output.
- 2026-03-10: Updated the CLI to detect GitHub transitions from either configured repo metadata or an `origin` GitHub remote, auto-persist the GitHub provider, and migrate the full local backlog with latest status and comments into GitHub issues. Added mocked provider regression coverage for both repo-configured and remote-detected transitions.
- 2026-03-10: Added a dedicated `backlog-sync github --force` CLI command that re-syncs already-mapped local backlog items to GitHub on demand. Validation passed in `tests/provider-behavior.ps1` with coverage for refreshed status/title/body/labels, reopened remote issues, and incremental migrated comments.
- 2026-03-10: Completed VS Code extension parity for the backlog-sync/GitHub migration runtime. `vscode-extension/scripts/copy-assets.js` now bundles the root `.agentx` scripts, `vscode-extension/src/commands/initialize.ts` now installs them into initialized workspaces, and `npm run package` regenerated `vscode-extension/agentx-8.2.6.vsix` with the updated bundled runtime.
- 2026-03-10: Strengthened the AgentX Auto contract so internal specialist phases are now explicitly required to honor the corresponding agent definition, templates, skills, boundaries, and validation gates. Updated `.github/agents/agent-x.agent.md` and `docs/WORKFLOW.md`; diagnostics are clean for `docs/WORKFLOW.md`, and the remaining `agent-x.agent.md` diagnostics are pre-existing markdown parser issues unrelated to the new rule.
- 2026-03-11: Completed the 8.2.7 release stamp across installers, docs, packs, extension metadata, and version-sensitive tests while tightening Windows PowerShell support to require `pwsh` 7.4+ and fixing the VS Code extension README Mermaid fence. Validation passed with `pwsh -File .\tests\test-install.ps1` (135/135), `npm test` in `vscode-extension/` (400 passing), and `npm run package`, producing `vscode-extension/agentx-8.2.7.vsix`.
- 2026-03-11: Fixed the Consulting Research runner gap in `.agentx/agentic-runner.ps1` by parsing multiline frontmatter constraints/boundaries, injecting `Output Types` and `Execution Steps` into the live system prompt, and explicitly instructing deliverable agents to create workspace artifacts before summarizing. Added regression coverage in `tests/agentic-runner-behavior.ps1`; focused validation passed at 32/32.
- 2026-03-12: Drafted harness evaluation adoption planning artifacts to define a phased, advisory-first evaluation layer with a canonical schema, deterministic check registry, passive artifact observers, scoring, attribution, and later reporting/gating integration. The durable architecture now lives in `docs/artifacts/adr/ADR-Harness-Evaluation-Adoption.md`, `docs/artifacts/specs/SPEC-Harness-Evaluation-Adoption.md`, and the shipped extension evaluator.
- 2026-03-12: Added the matching architecture artifacts at `docs/artifacts/adr/ADR-Harness-Evaluation-Adoption.md` and `docs/artifacts/specs/SPEC-Harness-Evaluation-Adoption.md`. The next implementation step is to define the canonical evaluation schema and first deterministic check registry, then reconcile harness maturity docs with the shipped runtime slice.
- 2026-03-12: Implemented the first evaluator slice in `vscode-extension/src/eval/`: deterministic checks over plan/progress/loop/harness artifacts, score plus coverage plus attribution summaries, Quality sidebar integration, and regression tests. Validation passed with `npm test` in `vscode-extension/`.
- 2026-03-12: Completed the follow-on product planning pass for the new knowledge-compounding and agent-native review initiative. Created `docs/artifacts/prd/PRD-157.md`, epic `#157`, features `#158`-`#161`, and stories `#162`-`#169`, then linked the hierarchy and updated epic/feature bodies to reference the PRD and real child backlog.
- 2026-03-12: Recommended implementation order for the new initiative is `#163` knowledge-compounding workflow contract, then `#162` learnings metadata/ranking contract, followed by retrieval wiring (`#166`, `#169`), agent-native review (`#165`, `#168`), and durable review-finding tracking (`#164`, `#167`).
- 2026-03-12: Completed story `#163` architect deliverables. Added `docs/artifacts/adr/ADR-163.md` and `docs/artifacts/specs/SPEC-163.md`, validated both files cleanly, and commented on GitHub issue `#163` with the artifact links and scope summary.
- 2026-03-12: Completed story `#162` architect deliverables immediately after `#163`. Added `docs/artifacts/adr/ADR-162.md` and `docs/artifacts/specs/SPEC-162.md`, validated both files cleanly, and commented on GitHub issue `#162` with the artifact links and schema/ranking summary.
- 2026-03-12: Implemented the first learnings retrieval and capture-guidance extension slice for stories `#166` and `#169`. Added curated learning artifacts under `docs/artifacts/learnings/`, added the shared guide `docs/guides/KNOWLEDGE-REVIEW-WORKFLOWS.md`, added deterministic ranking and capture guidance utilities plus new VS Code commands, chat routes, and Work view actions, and validated with `npm test` in `vscode-extension/` (414 passing).
- 2026-03-12: Implemented the first agent-native review slice for stories `#165` and `#168`. Added the shared guide `docs/guides/KNOWLEDGE-REVIEW-WORKFLOWS.md` and `docs/artifacts/learnings/LEARNING-165.md`, added deterministic parity evaluation plus new VS Code command, Quality sidebar rows, and chat route, and validated with `npm test` in `vscode-extension/` (418 passing).
- 2026-03-12: Completed the final durable review-findings slice for stories `#164` and `#167`. Added the shared guide `docs/guides/KNOWLEDGE-REVIEW-WORKFLOWS.md`, `docs/artifacts/reviews/FINDING-TEMPLATE.md`, curated learnings for `#164` and `#167`, a new `vscode-extension/src/review/review-findings.ts` model, new VS Code commands for showing and promoting findings, chat routes for `review findings` and `promote finding ...`, and sidebar summary/action wiring in both Work and Quality views.
- 2026-03-12: Reduced documentation clutter after rollout by merging the separate capture, agent-native review, and review-findings guides into `docs/guides/KNOWLEDGE-REVIEW-WORKFLOWS.md` and by deleting the completed harness evaluation plan/progress artifacts once the ADR, spec, and implementation were in place.
- 2026-03-12: Performed a more aggressive documentation cleanup pass by deleting the superseded archive `docs/architecture/ARCH-AgentX.md`, removing the unreferenced external Azure adoption review, and updating archival PRD/ADR/spec references plus doc-health metadata so the remaining docs point at live sources instead of dead architecture files.
- 2026-03-12: Hardened backlog promotion so review findings create normal AgentX issues through new base64-backed CLI inputs in `.agentx/agentx-cli.ps1`, then link the resulting issue number back into the durable finding record. Validation passed with `npm test` in `vscode-extension/` (424 passing).
- 2026-03-12: Fixed the remaining vendored markdown reference failures by rewriting packaged links in `vscode-extension/.github/agentx/Skills.md` and `vscode-extension/.github/agentx/docs/WORKFLOW.md` from repo-root `.github/...` paths to bundle-local `skills/...`, `agents/...`, and `templates/...` paths. `scripts/validate-references.ps1` now reports no `[HIGH]` failures; the remaining 232 `[LOW]` findings are pre-existing root-doc and skill-reference backlog outside the vendored packaging issue.
- 2026-03-12: Added a new original design skill at `.github/skills/design/design-system-reasoning/` to absorb useful external UI/UX patterns without copying. The skill focuses on design-direction synthesis, page archetype selection, domain anti-pattern filters, token guidance, and stack translation, and the repo skill counts were updated from 66 to 67 across the active docs.
- 2026-03-12: Enriched `design-system-reasoning` with second-pass depth: added an in-skill review checklist and industry-preset usage guidance, created `references/industry-presets.md`, created `assets/design-review-scorecard.md`, and expanded the brief template with preset selection and review-note fields.
- 2026-03-12: Strengthened the extension's compound-engineering loop by adding explicit brainstorm and compound-loop surfaces in commands, chat, and sidebars, plus a `create learning capture` action that scaffolds `docs/artifacts/learnings/LEARNING-<issue>.md` from the active harness thread context. Added utility renderers and tests for brainstorm guidance, compound-loop guidance, and sidebar exposure.
- 2026-03-12: Synchronized the public docs with the stronger compound loop. Updated `README.md`, `docs/GUIDE.md`, `docs/guides/KNOWLEDGE-REVIEW-WORKFLOWS.md`, `vscode-extension/README.md`, and `docs/artifacts/prd/PRD-157.md` so brainstorm, compound-loop inspection, learning-capture scaffolding, review surfaces, and the current 67-skill count are documented together.
- 2026-03-12: Started the repo-structure alignment slice. Moved the active plan/progress artifacts into `docs/execution/plans/` and `docs/execution/progress/`, added redirect shims for the legacy `docs/plans/` and `docs/progress/` paths, and began rewiring canonical workflow guidance to the new execution layout without changing the higher-risk artifact families yet.
- 2026-03-12: Completed the durable-artifact migration slice. Moved PRDs, ADRs, specs, reviews, and learnings under `docs/artifacts/`, rewired agent contracts, prompts, workflows, CLI checks, extension code/tests, and public docs to the canonical paths, and recreated the old top-level artifact folders as redirect-only shims.
- 2026-03-12: Reduced the post-migration markdown validator noise. Updated `scripts/validate-references.ps1` to ignore fenced code blocks and placeholder links, updated `vscode-extension/scripts/copy-assets.js` to emit compatibility root docs plus docs copies for bundled markdown, and cleared the remaining high-severity bundled `Skills.md` failures. The validator still reports low-severity debt from stale relative links and missing sample assets.
- 2026-03-12: Continued the markdown-debt cleanup. Fixed stale archival PRD/ADR/spec links, corrected internal-agent skill links, cleaned compatibility outputs before each asset rebuild, added compatibility rewrites for bundled `WORKFLOW.md`, and reduced validator output further to 63 low-severity findings with no remaining structural high-severity failures.
- 2026-03-12: Completed the markdown reference cleanup. Fixed the remaining UX/UI reference typos, updated bundled artifact-doc rewrites and compatibility tech-debt rewrites, added placeholder Databricks skill assets, regenerated the extension bundle, and reached `scripts/validate-references.ps1` = 0 broken links.
- 2026-03-13: Completed the next bounded extension structure slice by splitting `vscode-extension/src/utils/commandValidator.ts` into facade, types, policy, and helper modules while keeping the public import surface stable. Validation passed with `get_errors` on the touched files and `npm test` in `vscode-extension/` (`429 passing (21s)`).
- 2026-03-13: Completed the following bounded extension structure slice by splitting `vscode-extension/src/utils/harnessState.ts` into facade, types, and internal-storage/helper modules while keeping the public import surface stable. Validation passed with `get_errors` on the touched files and `npm test` in `vscode-extension/` (`429 passing (21s)`).
- 2026-03-13: Completed the next bounded extension structure slice by splitting `vscode-extension/src/utils/learnings.ts` into facade, types, and internal parsing/scoring helper modules while keeping the public import surface stable. Validation passed with `get_errors` on the touched files and `npm test` in `vscode-extension/` (`429 passing (22s)`).
- 2026-03-13: Completed the next bounded extension structure slice by splitting `vscode-extension/src/utils/dependencyChecker.ts` into facade, types, and internal probe/helper modules while keeping the public import surface stable. Validation passed with `get_errors` on the touched files and `npm test` in `vscode-extension/` (`429 passing (24s)`).
- 2026-03-13: Completed the next bounded extension structure slice by splitting `vscode-extension/src/utils/ssrfValidator.ts` into facade, types, and internal policy/helper modules while keeping the public import surface stable. Validation passed with `get_errors` on the touched files and `npm test` in `vscode-extension/` (`429 passing (29s)`).
- 2026-03-13: Completed the remaining major review-side hotspot in this cleanup tranche by splitting `vscode-extension/src/review/review-findings.ts` into facade, types, and internal parsing/persistence modules while keeping the public import surface stable. Validation passed with `get_errors` on the touched files and `npm test` in `vscode-extension/` (`429 passing (21s)`).
- 2026-03-21: Implemented summary-based context compaction in `.agentx/agentic-runner.ps1` while keeping the 70% threshold trigger unchanged, added runner regression coverage for merged summaries, and refreshed bundled extension assets with `vscode-extension/scripts/copy-assets.js`.
- 2026-03-21: Wired per-agent reasoning frontmatter into `.agentx/agentic-runner.ps1`. The runner now parses `reasoning` blocks from `.agent.md` files, resolves shorthand `Opus 4.6` to `claude-opus-4.6`, emits provider-specific request options for GPT-5 and Claude 4.6 models in Copilot mode, and passed targeted validation via `get_errors`, PowerShell parse checks, and runtime sanity checks for engineer and consulting-research agent mappings.
- 2026-03-13: Completed the final public-surface cleanup slice for this pass by extracting `vscode-extension/src/commands/initialize.ts` into a slim registration facade over `initializeCommandInternals.ts` and extracting `vscode-extension/src/utils/shell.ts` private helpers into `shellInternals.ts`. A stale leftover fragment in `initialize.ts` was caught by manual reread after the first patch, removed immediately, and final validation passed with `npm test` in `vscode-extension/` (`429 passing (21s)`).
- 2026-03-13: Confirmed via a production-only hotspot scan that the remaining larger public files are already thin facades or intentionally retained runtime surfaces; the mixed public-surface refactor pass is effectively complete, with only optional deeper internal decomposition left.
- 2026-03-18: Implemented the hidden runtime asset architecture. Initialize now copies `.github/agents`, `.github/templates`, and `docs/guides` into `.agentx/runtime/*`; extension agent/template/guide lookups and the PowerShell CLI now use hidden runtime defaults instead of requiring visible workspace copies. Validation: TypeScript compile emitted `TSC_OK`, the CLI `workflow engineer` smoke test passed, and the compiled mocha run showed the touched suites passing while four unrelated pre-existing tests still fail elsewhere in the repo.
- 2026-03-13: Finished the remaining public-surface cleanup pass by reducing `vscode-extension/src/commands/setupWizard.ts` to a facade, moving the remaining chat route handlers behind `requestRouterInternals.ts`, shrinking `vscode-extension/src/agentxContext.ts` through new internal runtime helpers, and adding direct tests for `runInitializeCommand`. Final validation passed with `npm test` in `vscode-extension/` (`431 passing (18s)`), and the public hotspot scan dropped `requestRouter.ts` below 100 lines while removing `setupWizard.ts` from the remaining top hotspots.
- 2026-03-13: Implemented the first engineering slice of feature `#227`. Added CLI task-bundle operations in `.agentx/agentx-cli.ps1`, thin VS Code command wrappers plus bundle facade/engine modules under `vscode-extension/src/taskBundles/`, command manifest wiring, new extension tests, and `tests/task-bundle-behavior.ps1`. Validation passed with `pwsh -NoProfile -File .\tests\task-bundle-behavior.ps1` (16 checks) and `npm test` in `vscode-extension/` (`441 passing (44s)`).
- 2026-03-13: Completed feature `#226`. Added CLI bounded-parallel operations in `.agentx/agentx-cli.ps1`, thin VS Code command wrappers plus parallel facade/engine modules under `vscode-extension/src/parallel/`, command manifest wiring, new extension tests, and `tests/bounded-parallel-behavior.ps1`. Validation passed with `tests/bounded-parallel-behavior.ps1` and `npm test` in `vscode-extension/`.
- 2026-03-13: Revalidated the engineering surface of epic `#215`. Verified phase-one workflow-guidance commands/chat/sidebar wiring and reran `tests/task-bundle-behavior.ps1`, `tests/bounded-parallel-behavior.ps1`, and the extension suite with passing results. Remaining work in the current tree is primarily closeout and review of draft epic-215 planning artifacts, not missing engineering implementation.
- 2026-03-13: Self-review for commit readiness passed on code quality gates: `npm test` in `vscode-extension/`, `tests/task-bundle-behavior.ps1`, `tests/bounded-parallel-behavior.ps1`, and `git diff --check` are clean. The remaining blocker to a single clean commit is scope: implementation files are mixed with draft planning artifacts and session memory updates, so the ready-to-commit slice should be split before committing.
- 2026-03-15: Completed the post-E2E remediation slice triggered by the external Advanced TODO App sandbox. Fixed installer core-doc bundling, wrapper exit-code leakage, `workflow` issue-type aliases, and `score-output.ps1` scalar/count/path robustness; added provider and installer regressions; reran `tests/provider-behavior.ps1` (42/42) and `tests/test-framework.ps1` (113/113). Optional follow-up: rerun a full external sandbox install after the next committed/released installer build to validate the packaged installer end to end.
- 2026-03-15: Follow-up installer smoke found and fixed fresh-install runtime-state leakage from the source `.agentx` directory. Both installers now exclude mutable source state, `tests/test-framework.ps1` passes 115/115 with the new static installer checks, and a local-archive external smoke install shows canonical docs/artifact directories present, local mode generated, reference validation clear of `[HIGH]` findings, and `score-output.ps1` executing without the old crash. The remaining unvalidated path is the published GitHub archive until these changes are committed or released.
- 2026-03-16: Removed the external Advanced TODO sandbox from the repo tree, completed the 8.3.1 release stamp, fixed the final stale extension fixtures for harness evaluation/sidebar quality, and reran `npm test` in `vscode-extension/` to green at `447 passing`. Remaining steps are final release-string sweep, commit, and push.
- 2026-03-16: Closed the confirmed stale-open GitHub issues (`#232`, `#215`, `#226`, `#227`, `#223`, `#224`, `#225`, `#228`, `#229`, `#230`, `#164`, `#165`, `#167`, `#168`, `#169`) and added prevention changes: closeout-keyword guidance, commit-hook warning, and the new `issue-closeout-audit.yml` workflow. Remaining open issues are the memory backlog cluster, which still appears to be real planned work.
- 2026-03-16: Applied the 8.3.2 release stamp across root installers/docs/manifests, pack manifests, extension metadata, bundled extension assets, and version-sensitive tests. Next steps are validation and the release commit.
- 2026-03-16: Fixed extension dependency-check UX drift in `vscode-extension/`: startup checks are now advisory-only, explicit environment setup remains available through the wizard, and PowerShell detection now probes standard Windows install paths to avoid false missing-tool prompts after install. Focused validation passed with `npm test -- --runInBand --grep "setupWizard|dependencyChecker"` (26 passing).
- 2026-03-16: Renamed the visible extension command `AgentX: Add Integration` to `AgentX: Config Advance`, added `AgentX: Add Plugin` with plugin-install scaffolding from the repo `.agentx/plugins` catalog, packaged `vscode-extension/agentx-8.3.7.vsix`, and committed the change as `a02fdd0`.
- 2026-03-16: Updated the shared AgentX runner and VS Code chat bridge so successful runs now emit a mandatory `[SELF-REVIEW SUMMARY]` block showing the completed `3/3` minimum review passes in final CLI output and preserved chat markdown previews. Focused validation passed with `tests/agentic-runner-behavior.ps1` (38/38) and the targeted chat suite (2 passing).
- 2026-03-17: Generalized durable advanced-runtime summaries in `.agentx/agentic-runner.ps1`, preserved `[EXECUTION SUMMARY]` blocks in chat previews, fixed chat routing so `@agentx config advance` and `@agentx add plugin` launch the extension commands directly, and applied the 8.3.8 release stamp across source, bundled assets, and version-sensitive tests. Validation passed with focused extension tests at `49 passing`; installer output confirms at least the updated 8.3.8 paths, but the full installer suite still needs a clean end-of-run confirmation before calling packaging fully verified.
- 2026-03-17: Replaced the mixed extension setup flow with a local-first command model. `AgentX: Initialize Local Runtime` now installs the repo-local runtime, `AgentX: Add Remote Adapter` configures GitHub or ADO separately, plugin and chat guidance now point runtime-dependent flows at local initialization first, and focused extension tests passed for chat, initialize, adapter, and plugin surfaces.
- 2026-03-17: Updated the CLI provider model so repo metadata and `backlog-sync github` no longer auto-promote GitHub over local mode. Added adapter-aware config reporting, refreshed provider behavior tests (41/41 passing), and rebuilt `vscode-extension/agentx-8.3.8.vsix` after the local-core adapter change.
- 2026-03-17: Updated the remote-detection behavior to match the latest product direction: initialization still installs the full local runtime, but GitHub and Azure DevOps origin detection now promotes the active workspace mode to `github` or `ado` instead of preserving `local`.
- 2026-03-17: Stamped tracked release surfaces to 8.4.0. Next step is rebuild/copy/package so bundled `.github/agentx`, compiled `out/`, and the VSIX all reflect 8.4.0 before commit and push.
- 2026-03-17: Investigated Problems-tab noise in the bundled extension CLI copy. Replaced PowerShell 7 null-coalescing syntax and automatic-variable `$args` assignments in both `.agentx/agentx-cli.ps1` and `vscode-extension/.github/agentx/.agentx/agentx-cli.ps1`; native PowerShell parsing now reports `count=0` errors for both files, and the earlier missing-brace/switch diagnostics were confirmed to be a cascade rather than real block corruption.
- 2026-03-17: Fixed the extension initialization false-negative for installed PowerShell 7 on Windows. `vscode-extension/src/utils/dependencyCheckerInternals.ts` now builds the version probe with a single-quoted inner PowerShell command and allows injected execution in tests; `vscode-extension/src/test/utils/dependencyChecker.test.ts` now covers the exact Windows quoting regression. Validation passed with `npm test -- --grep dependencyChecker` in `vscode-extension/` (11 passing).
- 2026-03-17: Added repo-level version automation and bumped the release to 8.4.1. `scripts/stamp-version.js` now syncs stamped release surfaces from tracked `version.json` at the repo root, `vscode-extension/package.json` runs it before compile/package, `auto-release.yml` applies it from the release output, targeted init/dependency tests passed at 34/34, and packaging produced `vscode-extension/agentx-8.4.1.vsix`.
- 2026-03-18: Slimmed `agentx.initializeLocalRuntime` to a minimal runtime manifest in the VS Code extension. The initializer now copies `.agentx` runtime scripts including the previously missing `agentic-runner.ps1`, copies `.github/agents`, creates the runtime artifact/session directories, stops bootstrapping git hooks/workflows/templates, and has a regression test locking the manifest. Validation: `npm run compile` passed in `vscode-extension/`; the new Initialize manifest test passed in isolation; a broader mocha run still shows four unrelated pre-existing failures in setup/chat/loop tests.
- 2026-03-19: Updated the root `README.md` with a new VS Code Marketplace setup section, workspace-by-workspace `AgentX: Initialize Local Runtime` guidance, optional remote-adapter setup, and a practical "Build Software With AgentX" flow with sample prompts. Validation: README diagnostics are clean.
- 2026-03-19: Completed deep plugin-platform research for publishing individual add-ons on top of AgentX core. Verified the current state: `agentx.addPlugin` installs from the AgentX source archive into workspace `.agentx/plugins`, `.github/schemas/plugin-manifest.schema.json` already defines a plugin contract, `packs/agentx-core` and `packs/agentx-copilot-cli` already establish a pack abstraction, and the main gaps are remote discovery, compatibility/version policy, trust/signing, and richer lifecycle/testing rather than basic plugin loading.
- 2026-03-18: Converted the plugin-platform research into architecture artifacts for issue `#234`. Added `docs/execution/plans/EXEC-PLAN-234-plugin-platform.md`, `docs/artifacts/adr/ADR-234.md`, `docs/artifacts/specs/SPEC-234.md`, and `docs/artifacts/learnings/LEARNING-234.md`; validated the architect handoff into `.agentx/handoffs/handoff-234-architect-to-engineer.json`; repo-wide link validation still reports pre-existing broken links outside this change set.
- 2026-03-19: Started implementation for issue `#234` in the VS Code extension. Added `vscode-extension/src/utils/pluginCatalog.ts` as the typed manifest/registry compatibility layer, routed `vscode-extension/src/commands/pluginsCommandInternals.ts` through it for manifest summaries, and added `vscode-extension/src/test/utils/pluginCatalog.test.ts` covering legacy manifests, v2 publication metadata, registry parsing, and AgentX version-range evaluation. Validation: targeted `npm test -- --grep pluginCatalog` passed (9 passing), and `npm run compile` passed in `vscode-extension/`.
- 2026-03-18: Fixed the loop-semantics root cause in repo tooling and docs. `.agentx/agentx-cli.ps1 loop status` now reports explicit completion-gate state, `vscode-extension/src/utils/loopStateChecker.ts` now distinguishes active/not-ready vs. minimum-met states, regression tests cover the new display text, and all live AGENTS quick references now say that 3 iterations do not equal completion until `loop complete` succeeds.
- 2026-03-19: Added the next issue `#234` implementation slice in the VS Code extension: `agentx.addPlugin` now prefers a registry-backed catalog, falls back to the legacy archive catalog when the registry is unavailable or empty, resolves host compatibility from `.agentx/version.json` or the extension version, and can install a published artifact using a `pluginPath` hint. Added initial registry metadata in `.agentx/plugins/registry.json` for the `convert-docs` plugin and regression tests for registry selection, fallback behavior, compatible release filtering, and artifact path resolution. Validation: `npm run compile` passed in `vscode-extension/`, targeted tests passed (15 passing), and the quality loop advanced to iteration `3/20`.
- 2026-03-19: Added the published-install hardening slice for issue `#234` in the VS Code extension. `agentx.addPlugin` now verifies registry artifact checksums before extraction, requires a modal trust/policy review for published installs, and appends install audit history to `.agentx/state/plugin-installs.json`. Added `pluginIntegrity.ts` and `pluginInstallState.ts`, expanded plugin manifest/schema support for publication metadata and permissions, upgraded the `convert-docs` plugin manifest to v2 metadata, and replaced the earlier monorepo bootstrap registry artifact entry with an independently published release-asset URL plus checksum metadata. Validation: `npm test -- --grep plugins` passed in `vscode-extension/` (8 passing, including compile).
- 2026-03-28: Normalized prompt resource guidance in `.github/prompts/` so code-review, refactor, test-gen, security-review, ux-design, devops, and architecture prompts now tell agents to read explicit repo-root skill/template files before acting. Validation passed with `scripts/validate-frontmatter.ps1` and `scripts/validate-references.ps1`.
- 2026-03-19: Removed the remaining sample-plugin treatment from the issue `#234` implementation. The bundled registry entry now matches the real published `convert-docs` identity (`jnpiyush.convert-docs`), `pluginsCommandInternals.ts` rejects registry/artifact identity mismatches before trust review or install, and focused plugin tests cover both the matching and mismatched cases. Validation: `npm run compile` passed in `vscode-extension/`, changed-file diagnostics were clean, and `npm test -- --grep plugins` passed in `vscode-extension/` (10 passing).
- 2026-03-19: Implemented stale-loop handling in CLI and extension. Verified CLI smoke path, stale-specific extension compile/tests, and isolated auto-reset behavior. Targeted test run still shows two unrelated pre-existing failures in harness evaluator scoring and agent minimum-tool baseline enforcement.
- 2026-03-19: Propagated the explicit pre-handoff loop-completion safeguard from reviewer-auto to engineer, reviewer, devops, and tester agent surfaces plus their Claude command summaries, and normalized the repo's last stale edit-tool wording to `apply_patch`-based guidance in project conventions.
- 2026-03-19: Seeded generic starter memory files through `.agentx/templates/memories` and wired copy-if-missing behavior into `install.ps1`, `install.sh`, and the VS Code `agentx.initializeLocalRuntime` flow. Validation passed with extension compile, the exact initialize test file including the new seed test, asset rebundling, installer static checks, and installer syntax checks; the lightweight framework suite still reports two unrelated pre-existing AI-engineer static failures.
- 2026-03-19: Aligned `packs/agentx-copilot-cli` with the wrapper runtime model. `--include-cli` now installs a hidden runtime bundle under `.github/agentx/.agentx`, seeds `.agentx` local state and starter memories, and writes workspace wrappers. Updated pack/root/extension docs and validated with real PowerShell and Bash installs into temp workspaces.
- 2026-03-20: Added source-of-truth domain skill coverage for CLM and corporate governance, updated published skill counts from 67 to 69, and queued extension asset rebundling plus skill validation.
- 2026-03-21: Diagnosed the remote `Dependency Scanning` failure to the `vscode-extension` Node audit gate. Fixed the explicit `flatted` override from `3.4.1` to `3.4.2`, regenerated the lockfile, and verified `npm install --package-lock-only` plus `npm audit --json` now report 0 vulnerabilities.
- 2026-03-21: Updated the VS Code chat clarification UX so streamed inter-agent clarification lines are also preserved as a compact `Clarification Discussion` markdown block in the user-visible chat transcript. Targeted `chatParticipant` tests passed at 23/23 and the extension compile remained clean.
- 2026-03-21: Updated the workflow and agent contracts so Architect performs a lightweight PM requirement-fit validation before handoff, and Engineer performs conditional design-alignment checks with Architect or Data Scientist when implementation crosses architecture or AI/ML boundaries.
- 2026-03-21: Wired the new cross-role checkpoints into the existing visible clarification path by teaching `.agentx/agentic-runner.ps1` to resolve `agents:` frontmatter collaborators as runtime clarification targets, updating Architect/Engineer/PM guidance to use that path, and validating the runner behavior suite at 53/53 passes.
- 2026-03-20: Created GitHub feature issue `#235` plus architecture artifacts `docs/artifacts/adr/ADR-235.md`, `docs/artifacts/specs/SPEC-235.md`, `docs/execution/plans/EXEC-PLAN-235-ai-eval-architecture.md`, and `docs/artifacts/learnings/LEARNING-235.md` to define an AgentX-owned AI evaluation contract with promptfoo-compatible and Azure-compatible runner paths.
- 2026-03-20: Implemented the first issue-235 code slice in the extension: `vscode-extension/src/eval/aiEvaluationContract*.ts` now loads and validates repo-local AI evaluation manifest, baseline, and normalized report artifacts, resolves runner selection, and is covered by targeted eval tests plus a clean extension compile.
- 2026-03-20: Implemented the second issue-235 code slice in the extension: `vscode-extension/src/eval/aiEvaluationRunner*.ts` now plans execution from validated contract state, executes injected adapters, normalizes runner output into the AgentX report shape, and persists normalized report artifacts. Validation passed with extension compile plus targeted contract/runner mocha suites.
- 2026-03-20: Continued issue `#235` with the first callable runtime slice. Added AI evaluation commands plus internals in `vscode-extension/src/commands/`, wired them into the registry and extension manifest, extended the eval manifest contract with optional execution config, scaffolded starter `evaluation/` artifacts, and validated with `npm run compile` plus targeted mocha coverage for the new commands.
- 2026-03-20: Pivoted issue `#235` from promptfoo-style integration toward lightweight prompt/eval practice adoption. Added `docs/guides/AI-EVALUATION-LIGHTWEIGHT.md`, root `prompts/` scaffolds, and root `evaluation/` starter assets, then fixed the docs index and verified the edited files with workspace diagnostics.
- 2026-03-20: Made the lightweight starter pack runnable by adding `scripts/run-ai-eval-sample.ps1`, wiring the root manifest to it, and validating that it emits the raw JSON shape expected by the extension while intentionally failing on the placeholder prompt and dataset rows.
- 2026-03-20: Replaced the generic starter prompt and dataset with a concrete AgentX issue-classification example, updated the sample evaluator to score exact label accuracy, and validated a clean 1.0/1.0 smoke result across five regression rows.
- 2026-03-20: Diagnosed the repeated VS Code extension activation failure from the VS Code extension-host log. The startup error was `Cannot find module 'yaml'` from the AI evaluation contract path.
- 2026-03-20: Removed `node_modules/**` from `vscode-extension/.vscodeignore` and rebuilt `agentx-8.4.9.vsix`; the rebuilt VSIX now explicitly includes `node_modules/yaml`.
- 2026-03-20: Hardened activation by deferring the `yaml` import inside `vscode-extension/src/eval/aiEvaluationContractInternals.ts`, so AgentX can activate even if that runtime package is missing from the installed payload.
- 2026-03-20: Reinstalled the rebuilt extension. Remaining user verification step: `Developer: Reload Window`, then confirm AgentX commands or sidebar load without the prior activation error.
- 2026-03-22: Fixed tester-reported stale expectations after the repo-wide loop minimum changed to 5. Updated the extension loop-start message and loop-state tests, made `tests/test-install.ps1` derive expected versions from root `version.json`, reran `vscode-extension/npm test` to 547 passing, reran `tests/test-install.ps1` to 138/138, reran `tests/test-framework.ps1` to 125/125, and restored the timestamp-only `version.json` test side effect.

- 2026-03-24: PSScriptAnalyzer fixes: renamed 21 unapproved verbs, fixed $profile automatic-variable collision, removed safe unused vars ($limit, $lessonsDir). Regression introduced by removing $
epo (dynamic scope) - fixed by adding explicit Get-ConfigValue fallback in Get-GitHubRepoSlug. Tests 49/49. Backlog.md smoke test passed end-to-end. Commit ae83325.
- 2026-03-27: Drafted `docs/execution/plans/EXEC-PLAN-harness-design-adoption.md` and `docs/execution/progress/PROGRESS-harness-design-adoption.md` to convert the Anthropic harness concepts into an AgentX-native rollout. The current direction is contract-driven `Work` slices, adaptive evaluator loops, and stronger runtime evidence inside the existing checkpoint system.
- 2026-03-27: Formalized the initiative into epic `#244`, created `docs/artifacts/prd/PRD-244.md`, `docs/artifacts/adr/ADR-244.md`, and `docs/artifacts/specs/SPEC-244.md`, then created and linked features `#245`-`#247` and stories `#248`-`#253`. Clean diagnostics passed on the new docs, and the AgentX quality loop was completed at 5 iterations.
- 2026-03-27: Completed story `#248` by adding `.github/templates/CONTRACT-TEMPLATE.md`, `docs/execution/contracts/README.md`, story-level execution artifacts, and workflow updates that keep bounded work contracts nested inside `Work`. Repo-wide reference validation returned to 0 broken links after fixing three stale execution-plan template links.
- 2026-03-27: Completed story `#249` by adding `.github/templates/EVIDENCE-SUMMARY-TEMPLATE.md`, `docs/execution/contracts/EVIDENCE-MODEL.md`, and workflow/execution doc updates that distinguish implementation, verification, and runtime evidence with durable locations. Touched-file diagnostics were clean, `scripts/validate-references.ps1` reported 0 broken links, and a completion comment was added to GitHub issue `#249`.
- 2026-03-28: Corrected harness-design story numbering drift to match the live backlog: the bounded contract docs are tracked under story `#250`, the evidence model under `#248`, the runtime contract-state code under `#253`, the reset-vs-compaction policy under `#249`, operator-surface guidance under `#251`, and pilot/pruning guidance under `#252`.
- 2026-03-28: Completed story `#253` by extending `vscode-extension/src/utils/harnessState*.ts` with contract lifecycle and slice-finding persistence, surfacing contract state in `workflowGuidance.ts` and the Work sidebar guidance tree, and validating with focused extension tests (`26` passing across harness state, workflow guidance, and work-tree guidance suites).
- 2026-03-28: Completed story `#249` with `docs/guides/RESET-VS-COMPACTION-POLICY.md` and workflow policy updates, completed story `#251` with shared contract-aware operator-surface guidance plus sidebar regression coverage, and completed story `#252` with `docs/guides/CONTRACT-HARNESS-PILOT-PRUNING.md`. Repo markdown reference validation remained clean at `0` broken links throughout.
- 2026-03-28: Added completion summary comments to stories `#248`-`#253`, parent features `#245`-`#247`, and epic `#244`, then completed the active AgentX quality loop at `5/20` after the full harness-design story backlog was implemented.
- 2026-03-28: Final release closeout for epic `#244` shipped as `8.4.13`. Reviewer and tester artifacts were added (`REVIEW-244.md`, `CERT-244.md`, `LEARNING-244.md`), installer E2E harness bugs were fixed in `tests/test-install.ps1`, release gates reran clean (`125/125` framework, `138/138` installer, `592` extension tests, coverage gate pass, audit-high pass, VSIX packaged), and commits `de08a0d`, `b0d4bba`, and `9bed71e` were pushed to `master`.
- 2026-03-28: Completed the frontmatter/parity audit remediation. Expanded `scripts/validate-frontmatter.ps1` to cover recursive instructions, prompts, `.github/copilot-instructions.md`, and `.claude/commands`; normalized `.claude/commands/*.md` into thin wrappers; moved detailed knowledge-review guidance into `docs/guides/KNOWLEDGE-REVIEW-WORKFLOWS.md`; added chat visibility bridges for task bundles and bounded parallel runs; expanded agent-native parity review coverage and tests; `validate-frontmatter.ps1`, `token-counter.ps1 -Action check`, and `npm test` in `vscode-extension/` all pass.
- 2026-03-28: Fixed the active Problems-tab warning slice in `.agentx/agentic-runner.ps1` by renaming analyzer-flagged helpers, replacing the terminal job `Invoke-Expression` path with `[scriptblock]::Create(...)`, and making empty catch blocks explicit. Validation passed with `tests/agentic-runner-behavior.ps1` (69/69), `get_errors` on the edited files, and ScriptAnalyzer no longer reports the targeted singular-noun, empty-catch, or `Invoke-Expression` findings for the root runner.
