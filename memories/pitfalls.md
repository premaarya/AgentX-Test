 2026-03-18: A hidden copied runtime still becomes stale after extension updates. For AgentX runtime/system assets, hiding copied defaults under `.agentx/runtime` is not enough; use installed assets plus explicit workspace-root injection instead of any copy-based default path for new workspaces.
- 2026-03-13: After moving an entire command body into an internals module, re-read the facade file before testing; stray lines from the old body can survive a large patch even when diagnostics do not immediately surface them.
- 2026-03-14: `gh issue create` requires `--body` in non-interactive mode; always pass `--body` with a fallback to the title when the body is empty, or `gh` exits with a cryptic "must provide --title and --body" error.
- 2026-03-13: In PowerShell strict mode, wrap candidate collections in `@(...)` before checking `.Count`; single-object returns from context-resolution helpers can otherwise throw property errors or bypass ambiguity guards.
- 2026-03-13: In PowerShell strict mode, JSON-derived records will also throw on missing optional properties like `isolation_mode`; read optional fields through `PSObject.Properties[...]` before normalizing them.
- 2026-03-14: Before "fixing" a checkpoint rule in workflowGuidanceEngine.ts, read the test first. `loopComplete -> Review` is correct per WORKFLOW.md: the loop completing IS the Work-to-Review signal. Misidentifying spec-correct behavior as a bug and reverting wastes a test cycle.
- 2026-03-15: For CLI integration tests, do not assert repo-version-specific or formatting-specific wrapper output when the copied test workspace may legitimately report `version unknown` or omit reordered fields. Assert the stable command contract instead: success exit code, non-empty output, and persisted provider state.
- 2026-03-15: Installer validation can miss fresh-install state leakage if it downloads a published archive instead of exercising the current repo tree. For installer regressions involving seeded `.agentx` state, run a local-archive smoke test against the current repo contents, not only the remote-download harness.
- 2026-03-16: Extension tests around harness evaluation can go red after doc-layout migrations even when runtime code is correct; check whether the fixture still seeds legacy `docs/plans` and `docs/progress` paths before changing evaluator logic.
- 2026-03-16: Plain commit references like `(#123)` or `refs #123` do not close GitHub issues. If work ships through direct commits to `master` without `fixes/closes/resolves`, issue state will drift even when comments and code show completion.
- 2026-03-16: PowerShell runner regressions that mock `Invoke-LlmChat` must return nested `PSCustomObject` payloads, not plain hashtables, because the runner reads `message.content` and `message.tool_calls` via object properties and otherwise treats the reply as an empty response.
- 2026-03-17: In PowerShell, using `+= ,@()` as a scope stack slot silently flattens the empty array; for nested runner state, store a hashtable/object wrapper instead of a raw empty array or ArrayList placeholder.
- 2026-03-17: VS Code PowerShell diagnostics can cascade on bundled scripts when they use PowerShell 7 null-coalescing syntax like `??`; replace it with compatible conditionals in packaged copies as well as the main source before treating the resulting missing-brace and switch errors as real syntax faults.
- 2026-03-17: When probing `pwsh` from Node on Windows through `child_process.exec` with `powershell.exe` as the shell, do not use a nested double-quoted `-Command "$PSVersionTable..."` probe; the outer shell expands `$PSVersionTable` first and makes valid PowerShell 7 installs look missing. Use a single-quoted inner command or a shell-safe version probe instead.
- 2026-03-19: Loop-state tests that hardcode old timestamps will start failing once stale-loop logic is added; use relative recent timestamps in fixtures unless the test is intentionally exercising staleness.
- 2026-03-19: Tests passing does not prove a large block replacement is structurally correct; verify both the removal of old identifiers and the presence of the new declaration.
- 2026-03-19: On Windows, `bash -n install.sh` can fail on a CRLF-encoded script even when the shell logic is valid; normalize carriage returns in-memory when doing a syntax-only validation pass from PowerShell.
- 2026-03-19: When generating PowerShell wrappers from installer code, escaped dollars inside single-quoted template lines survive into the output as literal backticks; run the generated wrapper at least once, not just the installer, to catch this class of failure.
- 2026-03-20: A `vscode-extension/.vscodeignore` rule that excludes `node_modules/**` can silently strip runtime dependencies from the VSIX; verify the packaged artifact contents from `vsce package` output instead of assuming local `npm ls` reflects the installed extension.
- 2026-03-20: `code --install-extension ... --force` did not reliably refresh the already-installed `out/` payload for the same version during local debugging. When logs and installed files disagree with the rebuilt workspace output, compare the installed `out/` file hashes directly and, if needed, overwrite the installed runtime files before asking the user to reload.
- 2026-03-21: When bulk-patching `.agent.md` frontmatter with `apply_patch`, anchor the edit below the opening `---` delimiter instead of replacing the top block header; otherwise the opening delimiter can be dropped across many files and break frontmatter validation.
- 2026-03-21: Before chasing transitive dependency upgrades, check workspace `package.json` `overrides`; a stale explicit override can pin a vulnerable version and make remote dependency scans fail even when upstream ranges allow a patched release.
- 2026-03-21: The agentic runner previously ignored agent frontmatter collaborator lists when building `canClarify`, so documented cross-role checkpoints could not execute at runtime even though the agent contracts looked correct; always validate runtime parsing against frontmatter-based contracts.
- 2026-03-22: Some framework checks assert exact phrases inside agent definition markdown, not just semantic guidance. When cleaning up `.agent.md` wording, grep the framework assertions first or doc-only edits can break the suite.
- 2026-03-22: For PowerShell commands that must emit clean JSON, do not inline-call helper scripts that use `Write-Host`; spawn them through a redirected child process and set the working directory explicitly or machine-readable output will be polluted and nondeterministic.
- 2026-03-22: Long-running installer regressions can look like mid-suite product failures when the terminal call times out near a scenario header; rerun the full suite with a generous timeout before debugging the installer itself.